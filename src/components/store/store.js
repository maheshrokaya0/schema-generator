import { writable } from "svelte/store"; 

export const tableList = writable([
    // {id: "1", name: "name1", fields:[]},
    // {id: "2", name: "name2", fields:[]},
]);
// console.log(tableList);


  